name: "Zed sccache Benchmark"
run-name: >-
  ${{ format('Benchmark {0} | {1}', github.event_name == 'workflow_dispatch' && inputs.cache_lane == 'rolling' && 'Rolling' || 'Fresh', github.event_name == 'schedule' && 'Scheduled' || github.event_name == 'pull_request' && format('PR #{0}', github.event.pull_request.number) || format('Manual dispatch ({0})', github.ref_name)) }}

on:
  workflow_dispatch:
    inputs:
      cache_lane:
        description: "Benchmark lane"
        required: false
        type: choice
        default: fresh
        options:
          - fresh
          - rolling
  schedule:
    - cron: "0 4 * * 5"
  pull_request:
    paths:
      - ".gitmodules"
      - ".github/workflows/zed-sccache-benchmark.yml"
      - ".github/workflows/zed-sccache-actions-cache.yml"
      - ".github/workflows/zed-sccache-boringcache.yml"
      - ".github/workflows/zed-sccache-depot-cache.yml"
      - "README.md"
      - "scenarios/**"
      - "scripts/**"
      - "upstream"

permissions:
  contents: read
  actions: write

jobs:
  github-actions-cache:
    name: GHA
    uses: ./.github/workflows/zed-sccache-actions-cache.yml
    with:
      cache_lane: ${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}
    secrets: inherit

  depot-cache:
    name: Depot
    uses: ./.github/workflows/zed-sccache-depot-cache.yml
    with:
      cache_lane: ${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}
    secrets: inherit

  boringcache:
    name: BC
    uses: ./.github/workflows/zed-sccache-boringcache.yml
    with:
      cache_lane: ${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}
    secrets: inherit
